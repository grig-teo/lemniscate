{
  "_comment": "Failure report — the command could not complete. `result` carries the error message and optionally the tail (~2 KB) of the build/run log so the user can see what went wrong.",
  "direction": "client-to-server",
  "frame": {
    "type": "command_result",
    "id": "cmd-abc123",
    "status": "failed",
    "result": {
      "error": "docker compose up failed with exit code 1"
    }
  }
}
