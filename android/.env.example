# Copy these values into android/local.properties (which is git-ignored).
# local.properties is also where the Android SDK location lives:
#
#   sdk.dir=/Users/<you>/Library/Android/sdk
#
# Base URL of the deployed lemniscate server (no trailing slash needed).
SERVER_URL=https://grig-teo.space/lemniscate
