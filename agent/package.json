{
  "name": "lemniscate-device-agent",
  "version": "0.4.3",
  "description": "Lemniscate local device agent — pairs with the server and runs commands over an outbound WebSocket tunnel",
  "type": "module",
  "bin": {
    "lemniscate-agent": "index.js"
  },
  "engines": {
    "node": ">=24.18.0"
  },
  "scripts": {
    "test": "node --test"
  },
  "license": "MIT",
  "dependencies": {
    "ws": "^8.18.0"
  }
}
